# 複製成 .env 後填入實際值，.env 不會被提交到版控

DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=order_db

PORT=5000

# 用 `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"` 產生
JWT_SECRET=

# 前端與 API 不同源時才需要設定，多個來源用逗號分隔
CORS_ORIGIN=
